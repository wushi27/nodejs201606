
#user  nobody;
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

	upstream  dfb3  {
		ip_hash;
		#server	172.16.2.233:8081 weight=5;
		#server 10.10.0.235:80;
		server	10.10.1.7:8181 weight=5;
		#server	172.16.20.59:8080 weight=5;
		#server	172.16.2.25:8080 weight=5;
		#server  127.0.0.1:8000 weight=5;
	}
	
	upstream  staticservice  {
		server 127.0.0.1:8000;
	}
	#dfb
    server {
        listen       8089;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;
		location /status {
			stub_status on;
		}
		
		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|js|css|html)$ {
			root html/;
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header Accept-Encoding "none";
			proxy_ignore_headers "Cache-Control" "Expires";
			if ( !-e  $request_filename ){
				proxy_pass  http://staticservice;
			}
		}
		
        location / {
			root   html;
			index  mainFrame.html;
			proxy_pass http://dfb3;
        }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }
	#didi
	upstream  didi  {
		ip_hash;

		server	10.10.1.149:8080 weight=5;
		server  127.0.0.1:8111 weight=5;
	}
	
	upstream  staticserviceDidi  {
		server 127.0.0.1:8111;
	}
	server {
        listen       8111;
        server_name  0.0.0.0;

        location / {
			root E:/activity/1/activityDeployFront/src/main/webapp;
            index  index.html;
        }
    }
    server {
        listen       8686;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;
		location /status {
			stub_status on;
		}
		
		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|js|css|html)$ {
			root html/;
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header Accept-Encoding "none";
			proxy_ignore_headers "Cache-Control" "Expires";
			if ( !-e  $request_filename ){
				proxy_pass  http://staticserviceDidi;
			}
		}
		
        location / {
			root   html;
			index  mainFrame.html;
			proxy_pass http://didi;
        }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }

    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  mainFrame.html;
    #    }
    #}

	server {
        listen       8000;
        server_name  0.0.0.0;

        location / {
			root E:/dfb3.0/kpayBackend/aggregate/src/main/webapp/;
            index  mainFrame.html;
        }
    }

    # HTTPS server
    #
    #server {
    #    listen       443;
    #    server_name  localhost;

    #    ssl                  on;
    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_timeout  5m;

    #    ssl_protocols  SSLv2 SSLv3 TLSv1;
    #    ssl_ciphers  HIGH:!aNULL:!MD5;
    #    ssl_prefer_server_ciphers   on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}

}
